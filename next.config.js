/** @ts-ignore */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "bignlean.shellcode.cloud",
      },
    ],
  },
};

module.exports = nextConfig;
